import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ujian Praktek Mobile P',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
